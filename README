	C++代码都在g++ edition 4.6.3运行成功，测试完成。

	算法具备模拟多个城市的特点（我设定了10个城市），具备任意设定初始化城市的距离，具备设定多少个城市作为初始随机种群（这里我设定了100个初始种群），具备设定多少代(这里我设定了100代)强制停止进化，具备设定多少次进行灾变（我设定了30次）。

	所有以上的具备都可以在代码的#define中重新定义。值得一提的是，在主函数main中，我添加了record（）函数作为记录每一代的输出情况，如需要调试，测试可以使用该函数。